'use strict';

if (process.env.NODE_ENV === 'production') {
  module.exports = require('scheduler/cjs/scheduler.production.min');
} else {
  module.exports = require('scheduler/cjs/scheduler.development');
}
